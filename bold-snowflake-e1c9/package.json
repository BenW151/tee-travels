{
  "name": "bold-snowflake-e1c9",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.js",
    "deploy": "npm run build && wrangler deploy dist/index.js",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "esbuild": "^0.23.0",
    "vitest": "1.5.0",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "mimetext": "^3.0.24"
  }
}
